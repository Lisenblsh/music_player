<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Player_java" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowTranslucentStatus">true</item>

        <item name="windowNoTitle">true</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

        <item name="android:textColor">@color/white</item>

    </style>

    <style name="Theme.Player_java.IconsTint" parent="Theme.Player_java">
        <item name="tint">@color/black</item>
        <item name="android:thumbTint">@color/black</item>
        <item name="android:progressTint">@color/black</item>
    </style>

    <style name="TaskBarTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/accent_night</item>
        <item name="tabBackground">@color/white</item>
        <item name="tabSelectedTextColor">@color/accent_night</item>
    </style>
</resources>